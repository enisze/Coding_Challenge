import React from "react";
import "./App.css";
import { NavSidebar } from "./Components/Navigation";
import { Router } from "./Pages/Router";

const App: React.FC = () => {
  return (
    <Router>
    </Router>
  );
};

export default App;
